#include "main.h"

/** 
 * mul - multiplies two numbers
 * @a: input number.
 * @b: input number
 * 
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
